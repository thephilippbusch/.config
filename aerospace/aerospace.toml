# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = ['exec-and-forget sketchybar']

# Notify Sketchybar about workspace change
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = "move-mouse window-lazy-center"

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 5
inner.vertical =   5
outer.left =       5
outer.bottom =     5
outer.top =        [{monitor.main = 5}, 38]
outer.right =      5

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-period = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
alt-shift-up = 'resize smart +50'
alt-shift-down = 'resize smart -50'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-m = 'mode move'
alt-r = 'mode service'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = ['focus left', 'mode main']
alt-shift-j = ['focus down', 'mode main']
alt-shift-k = ['focus up', 'mode main']
alt-shift-l = ['focus right', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-shift-1 = ['workspace 1', 'mode main']
alt-shift-2 = ['workspace 2', 'mode main']
alt-shift-3 = ['workspace 3', 'mode main']
alt-shift-b = ['workspace B', 'mode main'] # Browser
alt-shift-c = ['workspace C', 'mode main'] # Communication
alt-shift-d = ['workspace D', 'mode main'] # Docker
alt-shift-e = ['workspace E', 'mode main'] # Explorer (Finder)
alt-shift-g = ['workspace G', 'mode main'] # Gaming
alt-shift-m = ['workspace M', 'mode main'] # Mail
alt-shift-n = ['workspace N', 'mode main'] # Notes
alt-shift-s = ['workspace S', 'mode main'] # Spotify
alt-shift-t = ['workspace T', 'mode main'] # Terminal / Coding

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-shift-tab = ['workspace-back-and-forth', 'mode main']

# Disables macOS's native "hide application" shortcut
# See: https://nikitabobko.github.io/AeroSpace/goodness#disable-hide-app
cmd-h = []
# cmd-alt-h = []

[mode.move.binding]
# Exit mode on Escape
esc = 'mode main'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
h = ['move left', 'mode main']
j = ['move down', 'mode main']
k = ['move up', 'mode main']
l = ['move right', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#join-with
alt-h = ['join-with left', 'mode main']
alt-j = ['join-with down', 'mode main']
alt-k = ['join-with up', 'mode main']
alt-l = ['join-with right', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-monitor
shift-alt-h = ['move-node-to-monitor left', 'mode main']
shift-alt-j = ['move-node-to-monitor down', 'mode main']
shift-alt-k = ['move-node-to-monitor up', 'mode main']
shift-alt-l = ['move-node-to-monitor right', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-1 = ['move-node-to-workspace 1', 'workspace 1', 'mode main']
alt-2 = ['move-node-to-workspace 2', 'workspace 2', 'mode main']
alt-3 = ['move-node-to-workspace 3', 'workspace 3', 'mode main']
alt-b = ['move-node-to-workspace B', 'workspace B', 'mode main'] # Browser
alt-c = ['move-node-to-workspace C', 'workspace C', 'mode main'] # Communication (Teams, Slack, WhatsApp, etc.)
alt-d = ['move-node-to-workspace D', 'workspace D', 'mode main'] # Docker
alt-e = ['move-node-to-workspace E', 'workspace E', 'mode main'] # Explorer (Finder)
alt-g = ['move-node-to-workspace G', 'workspace G', 'mode main'] # Gaming
alt-m = ['move-node-to-workspace M', 'workspace M', 'mode main'] # Mail (Outlook)
alt-n = ['move-node-to-workspace N', 'workspace N', 'mode main'] # Notes (Obsidian)
alt-s = ['move-node-to-workspace S', 'workspace S', 'mode main'] # Spotify
alt-t = ['move-node-to-workspace T', 'workspace T', 'mode main'] # Terminal / Coding (Ghostty, VS Code, etc.)

# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
tab = ['move-workspace-to-monitor --wrap-around next', 'mode main']


# Toggle fullscreen
shift-f = ['fullscreen', 'mode main']

# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

# Workspace to monitor assignment
# [workspace-to-monitor-force-assignment]
# C = ['DELL', 'secondary', 'main']
# M = 'main'
# N = ['DELL', 'secondary', 'main']

# Automatic window detection
[[on-window-detected]]
if.app-id = "com.apple.finder"
run = "move-node-to-workspace E"

[[on-window-detected]]
if.app-id = "com.spotify.client"
run = "move-node-to-workspace S"

[[on-window-detected]]
if.app-id = "company.thebrowser.Browser"
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = "com.toggle.daneel"
run = "move-node-to-workspace 1"

[[on-window-detected]]
if.app-id = "com.mitchellh.ghostty"
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = "com.microsoft.teams2"
run = "move-node-to-workspace C"

[[on-window-detected]]
if.app-id = "com.electron.dockerdesktop"
run = "move-node-to-workspace D"

[[on-window-detected]]
if.app-id = "com.microsoft.VSCode"
run = "move-node-to-workspace T"

[[on-window-detected]]
if.app-id = "com.bitwarden.desktop"
run = "move-node-to-workspace 1"

[[on-window-detected]]
if.app-id = "md.obsidian"
run = "move-node-to-workspace E"

[[on-window-detected]]
if.app-id = "com.tinyspeck.slackmacgap"
run = "move-node-to-workspace C"

[[on-window-detected]]
if.app-id = "com.microsoft.Outlook"
run = "move-node-to-workspace M"

